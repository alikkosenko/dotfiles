" This is my vimrc file

" Set no compatible mode for vi
set nocp

" I want to see the numbers of lines in all my files for fast jumping with G command
set number

" Number of spaces which one tab occupies
set tabstop=4

" Now vim will make auto-indentation  
set autoindent

" Next lines will go with automatic one more level indentation 
set smartindent

" Setting UTF-8 encoding 
set encoding=utf-8

" Setting my favourite colorscheme
colorscheme industry 
